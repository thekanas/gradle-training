plugins {
    id 'java'
    id 'java-library'
}

repositories {

    flatDir {
        dirs "${project.rootDir}/utils/build/libs"
    }
}

version '1.0-SNAPSHOT'

dependencies {
    implementation project(":core")

}
